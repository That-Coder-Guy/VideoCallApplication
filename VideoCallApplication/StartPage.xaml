<local:NavigationPage x:Class="VideoCallApplication.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VideoCallApplication"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartPage">

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" Background="#66FFFFFF" CornerRadius="20,20,20,20" Padding="20,10,20,20">
            <StackPanel Width="200">
                <Label Content="Start" Margin="0,0,0,10" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="{DynamicResource HeaderFontSize}" FontFamily="{DynamicResource PrimaryFont}" Height="35"/>
                <Button Style="{DynamicResource RoundedButton}" x:Name="uxJoin" Content="Join" Margin="0,0,0,10" Click="OnJoinClick" Height="30" FontFamily="{DynamicResource PrimaryFont}" FontSize="{DynamicResource ButtonFontSize}"/>
                <Button x:Name="uxHost" Content="Host" Margin="0,10,0,0" Click="OnHostClick" Height="30" FontFamily="{DynamicResource PrimaryFont}" FontSize="{DynamicResource ButtonFontSize}" Style="{DynamicResource RoundedButton}"/>
            </StackPanel>
        </Border>

    </Grid>
</local:NavigationPage>
