<local:ClosablePage x:Class="VideoCallApplication.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VideoCallApplication"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClientPage" Closing="OnClosing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="13*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Border Background="Black"/>
        <Image/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button x:Name="uxStart" Content="Start" Height="30" Padding="50,1,50,1" Margin="0,0,25,0" Click="OnStartClick"/>
            <TextBox x:Name="uxAddressAndPort" TextWrapping="Wrap" Width="150" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</local:ClosablePage>
